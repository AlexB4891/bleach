% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/resumen.R
\name{sumarizado}
\alias{sumarizado}
\title{Resumir (agregar) variables por grupo, con o sin ponderación}
\usage{
sumarizado(tabla, grupo, variables, peso = NULL)
}
\arguments{
\item{tabla}{Un \code{data.frame}/\code{tibble} con las columnas de agrupación (\code{grupo}),
las variables numéricas a agregar (\code{variables}) y opcionalmente la columna de
pesos (\code{peso}).}

\item{grupo}{Character vector con los nombres de las columnas usadas para agrupar.
Debe contener nombres existentes en \code{tabla}. Puede ser \code{character(0)} (agrega
toda la tabla a un solo registro).}

\item{variables}{Character vector con los nombres de las columnas numéricas a
sumar. Deben existir en \code{tabla}.}

\item{peso}{\code{NULL} (default) o \code{character(1)} con el nombre de la columna de
pesos. Si se especifica, la suma se calcula como \code{sum(.x * tabla[[peso]], na.rm = TRUE)}.}
}
\value{
Un \code{tibble} con columnas de grupo y columnas agregadas para \code{variables}.
La salida está desagrupada (\code{.groups = "drop"}).
}
\description{
Calcula sumas por grupo para un conjunto de variables numéricas. Si se especifica
\code{peso}, la suma se calcula como \eqn{\sum x_i w_i}. La función retorna una tabla
con una fila por combinación de niveles en \code{grupo} y una columna por cada variable
en \code{variables} (con el mismo nombre original).
}
\details{
\itemize{
\item Con \code{peso = NULL}, para cada variable \code{v} en \code{variables} se calcula:
\code{sum(tabla[[v]], na.rm = TRUE)} por grupo.
\item Con \code{peso} no nulo, para cada variable \code{v} se calcula:
\code{sum(tabla[[v]] * tabla[[peso]], na.rm = TRUE)} por grupo.
}
}
